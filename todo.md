Here's a prompt for the task:

Task Title: Implement PWA (Progressive Web App) Features for Website

Description:

As part of the project requirements, we need to upgrade the website to a Progressive Web App (PWA). This will involve implementing features that enhance the user experience, such as offline functionality, faster loading times, and an app-like experience. The goal is to make the website work seamlessly across devices, even with intermittent or no internet connection.

Key Objectives:
Service Worker Implementation:

Add a service worker to handle caching and offline support.

Ensure the site works offline or with poor network conditions.

App Shell Architecture:

Implement an app shell model for faster loading and a consistent user interface.

Web App Manifest:

Create or update the web app manifest to include necessary metadata (such as name, icons, and theme color) so that users can install the app to their home screen.

HTTPS:

Ensure the website is served over HTTPS, which is a requirement for PWAs.

Push Notifications (Optional):

Implement push notifications to allow users to receive updates even when the app is not actively in use.

Performance Optimization:

Use performance best practices to reduce load times, such as lazy loading, code splitting, and image optimization.

Deliverables:
Service worker file for caching and offline functionality.

Updated manifest file.

HTTPS implementation (ensure the site is served over HTTPS).

Push notifications (optional, if applicable).

A detailed report on the steps taken to implement the PWA features.